import 'package:flutter/material.dart';

class HubVideoPage extends StatelessWidget {
  const HubVideoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("影视页\n"));
  }
}