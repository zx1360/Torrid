import 'package:flutter/material.dart';

class HubComic extends StatelessWidget {
  const HubComic({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("漫画页\n"));
  }
}