import 'package:flutter/material.dart';

class ProfileAbount extends StatelessWidget {
  const ProfileAbount({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("关于页详情"),
      ),
    );
  }
}