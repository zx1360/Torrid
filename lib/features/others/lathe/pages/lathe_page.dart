import 'package:flutter/material.dart';

class LathePage extends StatelessWidget {
  const LathePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Placeholder(),
    );
  }
}